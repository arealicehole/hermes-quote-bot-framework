# Stripe API key (use sk_test_ for sandbox, sk_live_ for production)
STRIPE_API_KEY=sk_test_xxx

# Database path (defaults to ./data/quotes.db)
QUOTES_DB_PATH=data/quotes.db

# MCP server port (defaults to 3207)
MCP_PORT=3207
MCP_HOST=127.0.0.1